defmodule Notes do
  @moduledoc """
  To create docmodule you have to put:

  defp deps do
    [
      {:ex_doc, "~> 0.12"}
    ]
  end

  in mix.exs, then you have to install it with mix deps.get
  To generate the html file you have to run mix docs

  """
end
